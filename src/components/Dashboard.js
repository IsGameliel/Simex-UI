import React from "react";

function Dashboard(){
    return(
        <div className="body">
            <h1>This will be the user dashboard for auth users</h1>
        </div>
    );
}

export default Dashboard;